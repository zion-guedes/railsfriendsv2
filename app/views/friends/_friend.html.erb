 <div class="jumbotron">
  <div id="<%= dom_id friend %>"> 
  <h1 class="display-4"><%= friend.first_name %> <%= friend.last_name %></h1>
  <p class="lead">
    <%= friend.email %> |
    <%= friend.phone %> |
    <%= friend.instagram %>    
  </p> 
  <hr class="my-4"> 
  <p class="lead">
     <%= link_to "Edit", edit_friend_path(@friend), class:"btn btn-secondary"  %> |
     <%= link_to "Back to list", friends_path, class:"btn btn-dark "  %>
  </p>
 </div>
</div>
